# Simple workflow for deploying static content to GitHub Pagesname: Deploy static content to Pageson:  # Runs on pushes targeting the default branch  push:    branches: ["main"]  # Allows you to run this workflow manually from the Actions tab  workflow_dispatch:# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pagespermissions:  contents: read  pages: write  id-token: write# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.concurrency:  group: "pages"  cancel-in-progress: false# Configure these variables under your repo settings.# You will likely need to set BASE_URL to be the name of your repo eg 'bentopdf'env:  SIMPLE_MODE: ${{ vars.SIMPLE_MODE }}  BASE_URL: ${{ vars.BASE_URL }}/   HUSKY: 0 jobs:  # Single deploy job since we're just deploying  deploy:    if: github.repository == 'boyranger/bentopdf'    environment:
